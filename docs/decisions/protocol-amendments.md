# Protocol Amendments

## 2026-09-24 - Pre-split family eligibility (Roadmap 11.3, 12.2)

Problem: 12.2 required a test-support check while 11.3 forbade test rows from influencing eligibility.
Resolution (user decision): eligibility is fixed before the split from corpus-wide support and component structure. The partitioner constructs the split to satisfy minimum fit and test support for every eligible client-family combination; only feasible combinations enter the plan. Test rows never change eligibility after the split.
No confirmatory results existed; no results invalidated.

## 2026-09-24 - Permutation-control null compatibility (Roadmap 27, 31.2)

Problem: the complementary-knowledge gate required a "null-compatible" permutation control without a decision rule. A placeholder (`permutation_null_max = 0.01`) was used during development and rejected the claim on a control of -0.010 (95% CI -0.016 to -0.004) against +0.162 for the real families.
Resolution (user decision): null compatibility is a 95% BCa equivalence criterion. The paired interval of the permutation-control complementary gain must lie within +/-0.03 absolute recall, the already predeclared complementary-knowledge practical-effect threshold. An interval entirely outside the band rejects the claim; an interval straddling a band edge, or an unavailable interval, leaves it INSUFFICIENT_EVIDENCE. No new threshold or ratio rule is introduced and `permutation_null_max` is removed.
No confirmatory results existed; no results invalidated.

## 2026-09-24 - Stage D fairness grids and hyperparameter freeze (Roadmap 16.3, 38)

Grids (predeclared in `configs/experiments.yaml`): local epochs {10, 20, 40}, fine-tuning epochs {2, 5, 10}, FedProx strength {0.001, 0.01, 0.1}. Selection rule: highest mean own-domain calibration-partition AUROC across development seeds and clients, ties to the smaller value; test rows never used, no new threshold. Result on development seeds 1-5: local epochs 10, fine-tuning epochs 2, FedProx strength 0.1. All three are grid-edge selections and the FedProx gap is small; grids were not extended. FedAvg rounds/local epochs and centralized epochs stay at their configured values (not searched).
Consequence: every development run made under the previous hyperparameters is stale (run provenance fingerprints the resolved training configuration) and is re-run under the frozen values. No confirmatory seed has been run.

## 2026-09-24 - Stage F protocol freeze (Roadmap 38)

Frozen before any confirmatory seed: data (LAMDA release var_thresh_0.01 fingerprint e9908f9a50ba1068a362f9fc943017afed2bb05dbccaaa1579d093c1ead677a9; AndroZoo metadata fingerprint f0d118739757cf9135830e2f20f444fb28adb426fe4a3c3329178c7084a97904), client definitions, family sets, metrics, statistical plan, the twelve claim gates (including the permutation equivalence rule), hyperparameters (local epochs 10, fine-tuning epochs 2, FedProx 0.1), the eight figures and seven tables, and confirmatory seeds 100 to 109. Configuration fingerprint 84ec9a19d140924ee72af71cf893bc6b0152e307cc9bf861319237b4d4d0ac83 at code revision dccf996; the confirmatory plan is 140 runs, none infeasible. Pre-confirmatory checks: doctor passes, the smoke run passes, the development rerun reproduces the frozen selection, and the confirmatory directories were empty. Any later change to a frozen item is a protocol amendment (Roadmap 47) and is recorded here.
Authorisation: the user instructed the confirmatory stages to proceed ("continue the implementation and don't stop until all is finished").

## 2026-09-24 - Post-confirmatory reassessment: correctness fixes and evidence-preserving analyses (Roadmap 20, 21, 28, 31.3, 39, 38 Stage L)

Scope statement: no original confirmatory arm, threshold, seed, family set, hyperparameter, or gate was changed. No run was retrained; every number below was recomputed from the stored confirmatory artifacts. Fresh seeds are not required. Roadmap text before this amendment has fingerprint 09a329281b63debcf29cd1c17c3ef519d38d14a717bf75314f53870957f36854 (recorded in the earlier `results/provenance/protocol.json`).

### Implementation defects corrected (the rule was already in the Roadmap)

1. Dose-response gate (Roadmap 20, 31.2). Old outcome: NARROWED, 1 of 3 criteria. Defect: the level table was filtered with `dose is not null`, which silently dropped the `all available` level (its requested dose is null), so the only level whose mean effective exposure reaches 100 peer samples (296.2) was never tested; the family-robustness test additionally used a different, row-level, exposure filter restricted to the 1000 level. Requested dose 100 realises a mean of only 9.0 effective samples and requested 1000 realises 79.4, so the criterion was unreachable under the old code. Correction: levels are grouped including the null-dose level, ordered by mean effective exposure, and the criterion, monotonicity and family-robustness tests all use the same qualifying levels. Thresholds (100 samples, +0.03, monotone tolerance 0.02) are unchanged. Corrected outcome: PROMOTED, 3 of 3 (all-available level: CTK gain 0.124 over zero dose; leave-one-family-out mean at least 0.089; recall non-decreasing within tolerance). An observation-level reading of the same criterion (the 70 FedAvg family-seed rows with at least 100 effective samples) also passes (mean gain 0.045; leave-one-family-out at least 0.040), so the outcome does not depend on the level-versus-observation ambiguity. Roadmap 20 now states the semantics. Affected artifacts: `claim-gates`, `peer-dose-response` table (now level-based with the all-available level labelled). Regression tests fail under the old implementation.
2. Family-dependence gate scope (Roadmap 31.2). Defect: FedAvg family gains were averaged across every experiment (natural scarcity, permutation control, sensitivities, dose runs), not the frozen family sets. Correction: only the primary (controlled-exposure) and replication family sets are used, threshold 0.10 unchanged. Outcome unchanged (PROMOTED): spread across the 15 frozen-set families is 0.362 (primary set 0.339, replication set 0.185). Regression tests added.
3. Micro-pooled robustness (Roadmap 28). Defect: the partition-salt sensitivity pooled its three salts as 30 pseudo-seeds (seed_count 30). Correction: salts are separate rows with 10 seeds each. The column previously called `mean_difference` is `micro_pooled_ctk_gain` with an `aggregation` label; the seed-paired estimand keeps its own name. Values for non-salt rows are unchanged.
4. Family-level table for natural scarcity classified every family as rescued when no full-exposure arm exists; it now reads not-classifiable.
5. Known-family-safety wording. Gate mechanics unchanged (strongest federated arm, as written). The stored wording now states that support is for the strongest federated arm only; arm-specific changes are tabulated.
6. Non-deterministic reporting order. Family-level novelty scores were averaged and correlated in a hash-dependent family order, so the feature-novelty bootstrap interval and 1e-15 float noise changed between regenerations (replication-set interval endpoints moved by up to 0.025; rho, p-values and the gate outcome did not). Scores are now ordered by family and the primary-arm table has a fixed column order. Stored rho, p-values, gate outcomes, seed-level metrics and all paired effects are bit-identical to the first promotion; only the feature-novelty bootstrap endpoints and micro-pooled BCa endpoints (resampling order, fourth decimal) differ.
7. Provenance. `results/provenance/code.json` recorded git HEAD (e079972, the freeze commit) while the working tree already contained the representation-limited wording change later committed in a61f0ed. Effect: wording only; no numeric evidence or gate outcome depended on it. Provenance now records `execution_revision` (last commit at or before the first confirmatory run was written; runs were written 19:44 to 21:05 on 2026-09-24; training-relevant code is identical between dccf996, e079972), `analysis_revision` (HEAD when analysis ran) and `analysis_sources_clean`. Historical revision values are not rewritten.

### Roadmap changes

| Section | Old intent | New wording | Reason | Changes a scientific degree of freedom | Changes an original result | Fresh seeds |
|---|---|---|---|---|---|---|
| 20 | Effective count is reported | Requested versus effective dose, all-available semantics | Ambiguity behind defect 1 | No (criteria unchanged) | Dose gate outcome corrected by defect 1 | No |
| 21 | Natural scarcity validates, not pooled | Named the principal external check; still not merged or gated | Clarify role | No | No | No |
| 28 | Robust = survives checks | Robust = persistence of sign and practical magnitude, not identical proportions | Clarify | No | No | No |
| 31.3 (new) | none | Evidence classes A to D; post-confirmatory analyses; arm-specific known-family wording; feature-novelty non-passage is unresolved, not disproven; bounded generic-pooling statement for the primary MLP; own-domain permutation caveat; family-dependence scope | Interpretation and reporting discipline | No | No | No |
| 38 Stage L, 39 | Publication figures | Includes post-confirmatory class C tables and figures; forest plot replaces the overlapping robustness figure | Reporting | No | No | No |

New class C analyses (evidence-preserving, existing runs only): local-baseline-anchored worst client, federated-arm trade-off, canonical robustness synthesis, family-level mechanism patterns, natural-scarcity side-by-side, permutation-control audit by population and alpha, mechanism headroom table, operating-point fidelity table.

## 2026-09-24 - Wording corrections, per-client analysis and novelty positioning (Roadmap H5, 20, 21, 31.3, 33, 42)

Scope statement: wording and class C additions only. No original confirmatory arm, threshold, seed, family set, hyperparameter or gate was changed; no original confirmatory number changed; no model was trained; no new seeds are required. Each row below: scientific degree of freedom changed: no; original result changed: no; fresh seeds: no.

| Section | Previous wording | New wording | Reason | Evidence source |
|---|---|---|---|---|
| 21 (and the 2026-09-24 amendment row for 21) | Natural scarcity is "the principal external check" of the controlled-exposure phenomenon | Principal within-dataset (ecological) validation; same LAMDA/AndroZoo corpus; not independent-dataset or external validation | Natural scarcity uses the same corpus; calling it external overstated it | `natural-scarcity-comparison.csv` (numbers unchanged) |
| 31.3 C | "elevated as validation of the controlled-exposure phenomenon" | "elevated as within-dataset (natural-exposure) validation ... not external validation" | Same | same |
| 42 Limited external replication | natural scarcity listed among internal replications | annotated as within-dataset | Same | none needed |
| H5 | single sentence: increase and diminishing returns | H5 declared compound; the dose gate tests only the increase; saturation unresolved | The promoted gate does not establish saturation; recall still rises at the largest effective exposure | `peer-dose-response.csv` (recall 0.437 to 0.562, no plateau) |
| 31.3 M (new) | none | Dose interpretation: increase supported, saturation not established | Keep the two parts of H5 apart | same |
| 31.3 L (new) | none | Per-client CTK analysis, class C, descriptive, no gate, interval only with at least 8 contributing seeds | Missing client-level evidence | `client-ctk-analysis.csv` from stored client metrics |
| 31.3 N (new), 33 | Pre-submission citation-chaining audit only | Bounded audit performed; no direct collision, three partial; "no closely matching study found in the audited literature" wording only; audit limits stated | Literature collision audit | `docs/Research Evidence.md` (section 29) |

The formal-interval rule in the per-client analysis reuses the existing `ctk_min_positive_seeds` value (8) as a minimum contributing-seed count; no new threshold is introduced, and rows below it are reported descriptively without an interval.

## 2026-09-25 - Per-client interval rule corrected (Roadmap 31.3 L)

Problem: the per-client analysis showed a BCa interval only for clients with at least 8 contributing seeds, justified by the gate's 8-of-10 positive-seed rule. That rule has nothing to do with whether a bootstrap interval is appropriate; it was an arbitrary and mis-justified reuse.
Correction: all clients are reported with their number of contributing seeds; intervals are labelled exploratory small-n BCa intervals and are shown whenever the existing helper can compute them (at least 3 seeds, non-constant effect), otherwise omitted (`interval-omitted-not-computable`). No threshold is used. The implementation requirement of the helper is documented separately from the scientific reading (intervals with 6 to 10 seeds and unequal support are unstable and are descriptive).
Effect: point estimates for every client are unchanged (maximum absolute difference 0.0); play-late now shows exploratory intervals (federation-wide CTK 0.328, 0.224 to 0.431). No original confirmatory result, gate, threshold, seed or family set changed; the analysis remains class C; no new seeds are required; no scientific degree of freedom changed.

## 2026-09-25 - Novelty positioning narrowed after the deeper audit; external-validity wording (Roadmap 33, 42, 31.3 L, N)

Wording only. Scientific degree of freedom changed: no. Original confirmatory result changed: no. Fresh seeds required: no.

| Section | Previous wording | New wording | Reason | Evidence source |
|---|---|---|---|---|
| 31.3 L | Per-client intervals "shown whenever the helper can compute one" (after the 2026-09-25 correction) | Adds that this is an implementation requirement, not a scientific threshold, and that intervals with 6 to 10 seeds are unstable and descriptive | Avoid implying precision | `client-ctk-analysis.csv` |
| 31.3 N, 33 | First audit: no direct collision, three partial | Second audit: no direct collision, four partial (adds Bi et al.); dose and natural scarcity described as partially anticipated; audit limits stated | Deeper reading of seven papers in full plus forward/backward chaining | `docs/Research Evidence.md` (section 29) |
| 42 Limited external replication | Independent validation named as the remedy | Adds the feasibility finding: no publicly verified independent corpus meets all requirements; replication is optional and gated on a support pre-check | Desk assessment of candidate datasets, none downloaded | `docs/Research Evidence.md` (sections 30 to 32) |

## 2026-09-25 - Extension mode, EXT-1, additional class C analyses, dataset decisions (Roadmap 31.3, 31.4, 42)

| Change | Reason | Literature motivation | Degree of freedom changed? | Original evidence affected? | Fresh seeds? | Study |
|---|---|---|---|---|---|---|
| New `ExecutionMode.EXTENSION`, seeds 200 to 209, `extension_experiments`, promotion to `results/extension/` | Run a prospective replication without touching the frozen study | Reviewers of FL and security papers expect clean negative controls and fresh-seed replication (Breitholtz et al., Zec et al. use 8 to 10 seeds with CIs) | No (original `run_fingerprint`, seeds, families, thresholds unchanged) | No | Yes (200 to 209) | extension (class D) |
| EXT-1: `family-permutation-control` replication, primary endpoint own-domain FedAvg CTK at 5% FPR inside +/-0.03 | Own-domain permutation interval extended past +0.03 in the original seeds | Misleading-certificate and matched-placebo concerns (Otani et al.; CELEST relatedness confound) | No (protocol frozen before execution; same code and hyperparameters) | No; original result reported as unresolved, extension as resolved, never pooled | Yes | extension (class D) |
| CTK variance components, family associations, per-client-by-family CTK | Quantify heterogeneity of CTK across families, seeds and clients | Family relatedness and client heterogeneity are named confounds (CELEST, CyberForce) | No (descriptive, stored runs) | No | No (existing seeds) | original study (class C) |
| Missing-class-aware baselines and a real placebo family not run | Label-space methods (FedRS, FedLC, FedVLS, FedMR, FedGELA) are undefined for a family inside the positive class of a binary detector; permutation control already restores malware volume | Full-text reading of those papers | No | No | n/a | none |
| KronoDroid not used (MARGINAL); McNdroid not used (81% overlap with LAMDA) | Feasibility gate applied to downloaded processed CSVs kept outside git | Independent-corpus replication needs benign samples, families, a client axis | No | No | n/a | none |
| `docs/Results.md`, `docs/Novelty Audit.md`, `docs/Second Dataset Feasibility.md` merged into `docs/Research Evidence.md` and deleted | Single evidence document | none | No | No | n/a | documentation only |

## 2026-09-25 - Extension-b protocols, Amendment A1, class C post-hoc analyses, full-text positioning update (Roadmap 31.3 F, M, N, O, P, Q; 31.4; 33; 42)

Scope statement: no original confirmatory arm, threshold, seed, family set, hyperparameter or gate was changed. The original 9 promoted, 0 narrowed, 3 rejected outcome stands. No original artefact was modified; `results/manifest.json` changed additively (new entries for new outputs).

| Change | Reason | Literature motivation | Degree of freedom changed? | Original evidence affected? | Fresh seeds? | Study |
|---|---|---|---|---|---|---|
| New mode `extension-b`; seed ranges EXT-LFAM 300 to 309, EXT-DOSE 310 to 319, EXT-CTRL 320 to 329; commands `large-family`, `dose-extension`, `controls-extension` | Run three prospective studies without touching the frozen study or EXT-1 | Reviewer-style gaps recorded in the evidence document (placebo, robust aggregation, exact dose, family-level explanation) | No (original `run_fingerprint`, seeds, families and thresholds unchanged) | No; never pooled with 100 to 109, 200 to 209 or each other | Yes | extension-b (class D) |
| EXT-LFAM frozen: 32 families chosen by an outcome-free rule from the seed-000 partition, single descriptor `nearest-known-family-distance`, Spearman with family-cluster bootstrap | H7 had 7 and 8 families and intervals spanning zero | CELEST and CyberForce invoke family relatedness as the explanatory variable | No (original gate untouched; power caveat fixed in advance) | No; feature-novelty gate stays "rejected" | Yes (300 to 309) | extension-b (class D) |
| EXT-DOSE frozen: exact effective dose {0, 10, 25, 50, 100, 200}, constant training volume, H-DOSE-1 and H-DOSE-2 | The original dose gate delivered far fewer effective samples than requested and never observed saturation | Exposure sweeps have precedent (positioning audit) | No (original dose gate promoted, unchanged) | No | Yes (310 to 319) | extension-b (class D) |
| EXT-CTRL frozen: coherent wrong-family placebo and robust aggregation (trimmed mean, median), H-CTRL-1 and H-CTRL-2 | Separate family specificity from generic malware volume; test rare-peer erasure | Matched-placebo confound (CELEST) and rare-peer erasure under robust aggregation (CyberForce) | No | No | Yes (320 to 329) | extension-b (class D) |
| Amendment A1, recorded before any seed 310 to 329 run: lower median (`median-lower`) as the second robust aggregator because the even-n median equals the trimmed mean with four clients; placebo reallocation with recorded shortfall; nested dose draws across levels; Holm over six dose levels and over two aggregators, one-sided Wilcoxon, explicit met rules | Implementation clarifications found before execution | none | No (hypotheses, populations, seeds and estimands unchanged) | No | n/a | extension-b (class D) |
| Freeze manifest `FREEZE.md` (sha256 of the four protocol files, pre- and post-A1) | Prove the order of freezing and execution | none | No | No | n/a | extension-b (class D) |
| New class C analyses: family x client x seed REML variance decomposition, aggregate-metric masking, negative-transfer decomposition (repair versus new capability); command `posthoc --promote` | Quantify where CTK heterogeneity lives, whether aggregate metrics hide the gain, and how much of CTK is repair | Misleading-certificate concern (Otani et al.); negative transfer (FedCollab) | No (descriptive, stored runs, no gate) | No | No (existing seeds) | original study (class C) |
| Positioning: full-text audit with a 34-row matrix (0 DIRECT, 9 PARTIAL, 22 ADJACENT, 3 NONE); FedRS, FedLMD, FedCKD, Ciaramella et al., Liu et al. (Cybersecurity 2026) and the PLOS One 2026 zero-day paper read; FID-SPA and FEDroid unavailable; stale "seven papers ... four partial" wording replaced | Deeper reading supersedes the earlier note | Contents of the audit | No | No | n/a | positioning only |
| Not carried out: McNdroid representation replication (development-seed proof of concept did not justify it), family-aware auxiliary objective, missing-class baselines, external datasets | Feasibility and applicability findings | FedRS, FedLC, FedVLS and others undefined for a family inside the positive class; KronoDroid MARGINAL; McNdroid overlaps LAMDA by 81% | No | No | n/a | none |

Dependencies: the class C analyses and extension outputs read stored runs and write to `results/evidence`, `results/tables` and `results/extension-b` only; `results/manifest.json` is additive.

## 2026-09-25 - EXT-REP representation replication, Amendment A2 (Roadmap 31.4; 33; 42)

Scope statement: no original confirmatory arm, threshold, seed, family set, hyperparameter or gate was changed. The original 9 promoted, 0 narrowed, 3 rejected outcome stands. No original artefact was modified; fingerprints of all legacy experiments are unchanged; `results/manifest.json` changed additively.

| Change | Reason | Literature motivation | Degree of freedom changed? | Original evidence affected? | Fresh seeds? | Study |
|---|---|---|---|---|---|---|
| New frozen protocol EXT-REP (Amendment A2), frozen in `FREEZE.md` before any EXT-REP partition, feature cache or run: SHA-aligned LAMDA/McNdroid overlap (554,136 rows), representations R0 to R3, H-REP-1 and H-REP-2, BCa, Holm, band +-0.03; explicitly not external validation | Test whether richer representations rescue families poorly detected under full exposure with LAMDA's 925 features | Representation-limited wording in the original study needed a representation contrast | No (original gate, threshold, families and seeds untouched) | No | Yes (330 to 339, mode `extension-b`) | extension-b (class D) |
| New experiments `representation-r0` to `representation-r3`; new CLI `representation-preprocess --mode extension-b` and `representation-extension --mode extension-b --promote`; new namespace `outputs/preprocessing/representation/`; outputs in `results/extension-b/` | Keep EXT-REP artefacts separate from the original and other extension outputs | none | No | No | Yes | extension-b (class D) |
| Retraction of the earlier "McNdroid representation replication not justified, not run" call (and the JSON-zeros reasoning) | The development-seed harness used a pooled threshold; the per-client-threshold harness matching the pipeline superseded it. Development numbers motivate the run and are not evidence | none | No | No | n/a | none |
| Result recorded: H-REP-1 met (static priority-macro +0.082, 0.053 to 0.132, 10/10 seeds); H-REP-2 not met under the frozen rule only because the static hiddad upper bound is wide (+0.013, -0.046 to 0.159); the frozen label "some representation rescues hiddad" is not supported by the data and is not claimed | Report the frozen rule faithfully and state the data-based reading | none | No | No | n/a | extension-b (class D) |
| Stated limitations: overlap not independent of LAMDA, provider-side feature selection, R2/R3 standardisation uses all fit rows including hidden-family rows, dimensionality differs | Transparency | none | No | No | n/a | extension-b (class D) |

## 2026-09-25 - Final verification pass: correctness corrections, Amendment A3, literature re-audit (Roadmap 31.3, 31.4; Research Evidence 29, 33.2)

Scope statement: no original confirmatory arm, threshold, seed, family set, hyperparameter, gate or original artefact was changed; the original 9 promoted, 0 narrowed, 3 rejected outcome stands. Every change below is a correctness correction, an implementation of an already frozen protocol element, a labelled sensitivity or a wording correction. No hypothesis, threshold, seed range or estimand was changed.

| Change | Reason | Degree of freedom changed? | Evidence affected | Study |
|---|---|---|---|---|
| **H-REP-2 interpretation logic (EXT-REP).** The frozen outcome map read "H-REP-1 met and H-REP-2 not met" as "some representation rescues hiddad". Failing a no-rescue criterion (no upper bound below +0.03) is not evidence of rescue. The hiddad status is now three-way: *rescue shown* (some representation's lower bound above +0.03, symmetric with H-REP-1), *no meaningful rescue* (all upper bounds below +0.03, the frozen H-REP-2), *unresolved* (otherwise); the combined outcome becomes `rep1-met-hiddad-unresolved` instead of `rep1-met-rep2-not-met` when the status is unresolved. The formal H-REP-2 status (met or not met) and all numbers are computed exactly as frozen | Logical defect in the frozen interpretation map (absence of equivalence read as presence of an effect) | No (formal rule and threshold unchanged; only the label for the not-met branch) | `representation-verdicts` (new `outcome` on the H-REP-2 row) | extension-b (class D) |
| **Amendment A3: EXT-REP per-arm training-only preprocessing, corrected rerun (user decision).** R2 (call graph) standardisation and R3 (report-JSON) column selection and standardisation were fitted on all fit rows of a seed partition, including hidden-family rows and other clients' rows, and shared by every arm; R3 candidate columns were additionally prefiltered on all overlap rows. Now every learned transform is fitted by each trained model from exactly its own training rows (local: that client's rows; central and FedAvg: the pooled rows of that arm, equivalent to federated sufficient statistics) and carried by the scorer to calibration and test; no prefilter. Measured before the fix: dropping one hidden family changed 1 to 24 of 1,419 selected columns and shifted call-graph log-SD by up to 7.8 for columns nearly constant without that family (`docs/temp/rep-leak/`). R2 and R3 were rerun with the frozen seeds 330 to 339; R0 and R1 have no fitted transform and their runs were reused unchanged (provenance identical). Previous R2/R3 run directories, partition scale files and promoted EXT-REP tables are kept as superseded in `outputs/superseded/ext-rep-transductive-transform/` and `results/extension-b/superseded/ext-rep-transductive-transform/` (with `SHA256SUMS`) | Exposure leakage into family-absent and local arms; correctness correction | No (seeds, families, partitions, models, thresholds, metrics, hypotheses, statistics and budgets unchanged) | all R2/R3 rows of `representation-*`; H-REP-2 inputs | extension-b (class D) |
| **EXT-DOSE monotonicity.** H-DOSE-1 required family-macro CTK non-decreasing across levels (Amendment A1) but the code compared only the top and bottom levels. Now every consecutive exact level is checked. Headline pooled, primary and federation-wide verdicts unchanged; replication-set own-domain at 1% FPR changes from met to not met (0.0102 at 25 samples, 0.0088 at 50) | Implementation did not match the frozen rule | No | `exact-dose-verdicts` (one secondary cell) | extension-b (class D) |
| **EXT-LFAM frozen statistics completed.** The frozen protocol names an exact permutation p and a seed-level rho distribution; the code reported scipy's asymptotic p and no seed-level rho. Added a permutation p (32! orderings cannot be enumerated: 9,999 Monte Carlo permutations of the descriptor over families, fixed stream, p = (1 + extreme) / (1 + draws)) = 0.026 (asymptotic 0.027), and the seed-level rho distribution (10 seeds: median 0.22, range -0.06 to 0.43, 9 of 10 positive). The frozen rule (interval above 0) and its result are unchanged | Implement protocol elements that were missing | No | `large-family-summary` (added rows) | extension-b (class D) |
| **EXT-LFAM eligibility-stability sensitivity (no training).** 27 of the 32 frozen families are structurally eligible in all 10 fresh seeds (umpay 0.2, anydown 0.6, gumen 0.7, feiwo and utchi 0.8); rho on the 27 stable families 0.274 (95% CI -0.099 to 0.581). Labelled sensitivity; the frozen 32-family result stays primary | Requested audit of whether the seed-000 eligibility is stable on fresh seeds | No | new `large-family-eligibility-stability{,-seeds,-summary}` | extension-b (class D) |
| **Promotion provenance.** Every manifest entry now carries an evidence class (`A-B-C-confirmatory-seeds-row-labelled`, `C-post-confirmatory-evidence-preserving`, `D-prospective-extension`); `results/extension/` and `results/extension-b/` have their own `manifest.json` with digests. The existing root manifest was relabelled in place (digests unchanged) | Extension and class C outputs were unlisted or unlabelled | No | manifests only | all |
| **Literature re-audit.** Verdict rule replaced by an identification-based rule (Research Evidence 29.2); 34-row 0/9/22/3 becomes 41-row 0/5/33/3 (FLEKD-IDS and, provisionally from secondary descriptions, Popoola et al. added as PARTIAL; PROTEAN, ZAID, an FGCS 2025 federated IDS and a Sci. Rep. 2025 paper added as ADJACENT; FedVLS, Breitholtz/Zec, Bi, FedP3E, MAP, Ishfaq from PARTIAL to ADJACENT); CyberForce's unmatched absent-everywhere arm and peer-holder sweep narrow N1 to the matched decomposition | Earlier rule scored problem overlap plus any baseline; primary-text re-reading | No | positioning only | none |
| **Wording.** Class C heterogeneity, masking and repair-versus-new-capability wording narrowed to descriptive and accounting language; "resolved in v2" replaced by "resolved prospectively in EXT-1"; technical tree labels class C tables as class C | Stronger wording than the analyses permit | No | documentation only | none |
| **A3 result.** Corrected R2/R3 evidence replaces the superseded tables. McNdroid-static rows are identical (R0/R1 runs reused). Call graph priority-macro +0.006 (-0.035, 0.036) (superseded +0.013), hiddad -0.055 (-0.103, -0.023); report-JSON priority-macro -0.132 (-0.176, -0.064) (superseded -0.121), hiddad -0.164 (-0.248, -0.102); FedAvg CTK priority-macro R2 0.206 (superseded 0.208), R3 0.108 (0.104). H-REP-1 met; H-REP-2 not met; hiddad status unresolved; outcome `rep1-met-hiddad-unresolved`. No conclusion changed; the A2 "standardisation uses all fit rows" limitation no longer applies | Correctness rerun | No | `representation-*` | extension-b (class D) |

## 2026-09-25 - Scientific convergence pass (Research Evidence 1, 29, 33.4, 34 to 38)

Scope statement: no original confirmatory artefact, gate, threshold, seed or family set changed (9 promoted, 0 narrowed, 3 rejected). No new experiment was run; every new result is a class C analysis of stored runs.

| Change | Reason | Degree of freedom changed? | Evidence affected | Study |
|---|---|---|---|---|
| **Determinism of extension and class C bootstraps.** `group_by` without `maintain_order` in `hidden_family`, `representation_extension`, `extension_effects`, `dose_extension` and `large_family` reordered seed vectors between runs, so position-based bootstrap draws changed confidence bounds (up to 0.035 in `negative-transfer-decomposition`, up to 6e-5 in `representation-levels`) and reordered effect tables. Fixed; regeneration is byte-identical across runs. Promoted class C and extension tables regenerated; means unchanged, some third-decimal interval bounds in Research Evidence 5.1 updated | Reproducibility defect found in the clean-copy regeneration | No | `negative-transfer-decomposition`, `representation-levels`, extension effect tables (row order) | class C, extension-b |
| **Diagnostics command** `diagnostics --mode extension-b`: EXT-REP equal-FPR reading, EXT-LFAM influence, EXT-DOSE curve shape and descriptive fits, EXT-CTRL strata, family synthesis | Characterise the frozen extension results without new gates | No (descriptive; frozen verdicts unchanged) | new `diagnostics-*` tables | class C on extension data |
| **Extension provenance** `results/extension-provenance.json` (protocol hash, config and data fingerprints, code revision, seeds, class, artefacts, superseded artefacts per study); `results/provenance/*.json` left untouched | Complete extension provenance without altering the original record | No | new file | all extensions |
| **Literature audit at that pass.** 41 rows, 0/5/33/3: Popoola et al. was provisionally classified PARTIAL from secondary descriptions; PROTEAN, ZAID and an FGCS 2025 federated IDS were added as ADJACENT; CyberForce's robust-aggregator comparison on the absent malware recorded as precedent | Final primary-text reading and citation chaining at that time | No | positioning only | none |

## 2026-09-25 - Popoola et al. primary-manuscript classification correction (Research Evidence 29)

The accepted-version author manuscript for Popoola et al. (DOI 10.1109/JIOT.2021.3100755) was retrieved and read after the prior pass. It confirms deliberate per-client class omission (A) and class-specific federated metrics (B), but the local-only comparison reports aggregate client metrics rather than the omitted class, so the class-specific exposure contrast (C) is not established under the stated rule. Reclassify PARTIAL to ADJACENT. The final 41-row counts are 0 DIRECT, 4 PARTIAL, 34 ADJACENT and 3 NONE. This changes positioning counts only; no experimental result, protocol, gate or claim outcome changes.
| **Documentation consolidated.** Research Evidence sections 1, 34 to 38 rewritten as final state (claims, contribution ranking, experiment assessment, limitations) instead of appended history | Remove repeated audit iterations | No | documentation only | none |
